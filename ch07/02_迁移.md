
```js
    docker run --rm --name some-mongo -d -p 27017:27017   \
            -v /mgdata/db:/data/db      \
            -e MONGO_INITDB_ROOT_USERNAME=mongoadmin -e MONGO_INITDB_ROOT_PASSWORD=secret mongo --auth --profile 1 --slowms 100
```

参考[迁移脚本](migration.sh)。